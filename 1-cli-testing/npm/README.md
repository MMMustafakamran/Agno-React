# npm CLI test

Run from this folder:

```bash
npx copilotkit@latest create
npx copilotkit@latest whoami
npx copilotkit@latest project select
npm install
npm run dev
```

Use a separate generated project name such as `cli-test-npm`.
